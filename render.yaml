services:
  - type: worker
    name: farm-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    disk:
      name: farm-data
      mountPath: /data
      sizeGB: 1
